global:
  appName: vitals-ch
  environment: sandbox
  storageClass: csi-hostpath-sc

replicaCount: 1

shards: 1

serviceAccount:
  create: true
  name: vitalsch

podSecurityContext:
  enabled: false

auth:
  username: vitals
  password: 111111

persistence:
  storageClass: csi-hostpath-sc
  size: 10Gi

zookeeper:
  enabled: false
