global:
  storageClass: csi-hostpath-sc

replicaCount: 1

shards: 1

serviceAccount:
  create: true
  name: lupi-ch

podSecurityContext:
  enabled: true

auth:
  username: lupi
  password: 111111

persistence:
  storageClass: csi-hostpath-sc
  size: 10Gi

zookeeper:
  enabled: false
